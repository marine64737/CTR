spring.application.name=CTR
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
spring.datasource.url=jdbc:mysql://localhost:3307/ctr?rewriteBatchedStatements=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=1234
#server.servlet.context-path=/ctr
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true
#spring.security.user.name=Kim
#spring.security.user.password=1234
server.servlet.session.timeout=1440m
server.port=8080
# Session timeout. If a duration suffix is not specified, seconds is used.
spring.session.redis.flush-mode=on_save
#Sessions flush mode.
# spring.session.redis.namespace=spring:session
# spring.session.jdbc.initializer.enabled=false
#logging.level.org.springframework.session=DEBUG
spring.session.store-type=redis
# Namespace for keys used to store sessions.
spring.data.redis.host=localhost
#spring.data.redis.host=redis
# Redis server host.
#spring.data.redis.password=1234
# Login password of the redis server.
spring.data.redis.port=6379
# Redis server port.

# server.servlet.session.cookie.path=/
# server.servlet.session.cookie.http-only=true
# ?? HTTPS? ? ??? ?? ??? false?? ???.
# server.servlet.session.cookie.secure=false
# spring.session.servlet.cookie-name=SESSION
# server.servlet.session.cookie.name=SESSION
# spring.session.servlet.add-filter=true
# spring.session.redis.repository-type=indexed

# 1. SameSite ??? ??? ?? (?? ??????)
# server.servlet.session.cookie.same-site=lax